## Get the list of words for which to download wikitionary pages
Will produce a directory containing a `words.json` and a `metadata.json`.
Give `./start.sh` a volume to save the data to and the targeted language.
